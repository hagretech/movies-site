new WOW().init();

// Global variabels
var moviesList = $('.movies__list'),
    active = '';

for (i=0; i < moviesList.length;i++){
    console.log(moviesList[i])
};

// event listner for the movies animation